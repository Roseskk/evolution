import styled from 'styled-components';

export const CardsWrapper = styled.ul`
  margin: 0;
  padding: 0;
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  align-items: center;
  gap: 2px;

  list-style: none;
`