import React from 'react';
import {CardsWrapper} from "../../../styles/cards.ts";


const BoardCards = () => {
    return (
        <CardsWrapper>
            {

            }
        </CardsWrapper>
    );
};

export default BoardCards;